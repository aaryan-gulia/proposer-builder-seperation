use crate::blockchain_env::*;
use crate::entities::*;
use rand::thread_rng;
use rand_distr::{Distribution, Normal, NormalError};

pub fn execute_simulation(num_blocks: u32) -> Vec<block::Block> {
    let mut blockchain: Vec<block::Block> = vec![];

    blockchain
}
