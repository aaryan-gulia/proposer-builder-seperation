<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="294" onload="init(evt)" viewBox="0 0 1200 294" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="294" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="277.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="277.00"> </text><svg id="frames" x="10" width="1180" total_samples="313"><g><title>libsystem_c.dylib`0x7ff80250a41b (1 samples, 0.32%)</title><rect x="0.0000%" y="197" width="0.3195%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="1"/><text x="0.2500%" y="207.50"></text></g><g><title>dyld`dyld4::RuntimeState::_finalizeListTLV (1 samples, 0.32%)</title><rect x="0.0000%" y="181" width="0.3195%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="1"/><text x="0.2500%" y="191.50"></text></g><g><title>dyld`invocation function for block in dyld4::RuntimeState::_finalizeListTLV (1 samples, 0.32%)</title><rect x="0.0000%" y="165" width="0.3195%" height="15" fill="rgb(221,193,54)" fg:x="0" fg:w="1"/><text x="0.2500%" y="175.50"></text></g><g><title>pbs_test`std::sys::unix::thread_local_dtor::register_dtor::run_dtors (1 samples, 0.32%)</title><rect x="0.0000%" y="149" width="0.3195%" height="15" fill="rgb(248,212,6)" fg:x="0" fg:w="1"/><text x="0.2500%" y="159.50"></text></g><g><title>pbs_test`alloc::sync::Arc&lt;T,A&gt;::drop_slow (1 samples, 0.32%)</title><rect x="0.0000%" y="133" width="0.3195%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="1"/><text x="0.2500%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`_free (1 samples, 0.32%)</title><rect x="0.0000%" y="117" width="0.3195%" height="15" fill="rgb(232,128,0)" fg:x="0" fg:w="1"/><text x="0.2500%" y="127.50"></text></g><g><title>libdyld.dylib`0x7ff802651e43 (4 samples, 1.28%)</title><rect x="0.0000%" y="213" width="1.2780%" height="15" fill="rgb(207,160,47)" fg:x="0" fg:w="4"/><text x="0.2500%" y="223.50"></text></g><g><title>libsystem_kernel.dylib`__exit (3 samples, 0.96%)</title><rect x="0.3195%" y="197" width="0.9585%" height="15" fill="rgb(228,23,34)" fg:x="1" fg:w="3"/><text x="0.5695%" y="207.50"></text></g><g><title>libsystem_malloc.dylib`_szone_free (7 samples, 2.24%)</title><rect x="24.6006%" y="133" width="2.2364%" height="15" fill="rgb(218,30,26)" fg:x="77" fg:w="7"/><text x="24.8506%" y="143.50">l..</text></g><g><title>libsystem_kernel.dylib`madvise (7 samples, 2.24%)</title><rect x="24.6006%" y="117" width="2.2364%" height="15" fill="rgb(220,122,19)" fg:x="77" fg:w="7"/><text x="24.8506%" y="127.50">l..</text></g><g><title>pbs_test`core::slice::sort::insertion_sort_shift_left (2 samples, 0.64%)</title><rect x="37.3802%" y="69" width="0.6390%" height="15" fill="rgb(250,228,42)" fg:x="117" fg:w="2"/><text x="37.6302%" y="79.50"></text></g><g><title>pbs_test`core::slice::sort::insertion_sort_shift_left (4 samples, 1.28%)</title><rect x="39.9361%" y="53" width="1.2780%" height="15" fill="rgb(240,193,28)" fg:x="125" fg:w="4"/><text x="40.1861%" y="63.50"></text></g><g><title>pbs_test`core::slice::sort::recurse (33 samples, 10.54%)</title><rect x="33.5463%" y="85" width="10.5431%" height="15" fill="rgb(216,20,37)" fg:x="105" fg:w="33"/><text x="33.7963%" y="95.50">pbs_test`core::..</text></g><g><title>pbs_test`core::slice::sort::recurse (19 samples, 6.07%)</title><rect x="38.0192%" y="69" width="6.0703%" height="15" fill="rgb(206,188,39)" fg:x="119" fg:w="19"/><text x="38.2692%" y="79.50">pbs_test..</text></g><g><title>pbs_test`core::slice::sort::recurse (9 samples, 2.88%)</title><rect x="41.2141%" y="53" width="2.8754%" height="15" fill="rgb(217,207,13)" fg:x="129" fg:w="9"/><text x="41.4641%" y="63.50">pb..</text></g><g><title>pbs_test`core::slice::sort::insertion_sort_shift_left (9 samples, 2.88%)</title><rect x="41.2141%" y="37" width="2.8754%" height="15" fill="rgb(231,73,38)" fg:x="129" fg:w="9"/><text x="41.4641%" y="47.50">pb..</text></g><g><title>libsystem_platform.dylib`DYLD-STUB$$_platform_memset (1 samples, 0.32%)</title><rect x="44.7284%" y="69" width="0.3195%" height="15" fill="rgb(225,20,46)" fg:x="140" fg:w="1"/><text x="44.9784%" y="79.50"></text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (1 samples, 0.32%)</title><rect x="45.0479%" y="69" width="0.3195%" height="15" fill="rgb(210,31,41)" fg:x="141" fg:w="1"/><text x="45.2979%" y="79.50"></text></g><g><title>pbs_test`core::hash::BuildHasher::hash_one (1 samples, 0.32%)</title><rect x="45.3674%" y="69" width="0.3195%" height="15" fill="rgb(221,200,47)" fg:x="142" fg:w="1"/><text x="45.6174%" y="79.50"></text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (1 samples, 0.32%)</title><rect x="45.3674%" y="53" width="0.3195%" height="15" fill="rgb(226,26,5)" fg:x="142" fg:w="1"/><text x="45.6174%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`_nanov2_free (1 samples, 0.32%)</title><rect x="46.0064%" y="53" width="0.3195%" height="15" fill="rgb(249,33,26)" fg:x="144" fg:w="1"/><text x="46.2564%" y="63.50"></text></g><g><title>libsystem_platform.dylib`_platform_bzero$VARIANT$Haswell (1 samples, 0.32%)</title><rect x="46.0064%" y="37" width="0.3195%" height="15" fill="rgb(235,183,28)" fg:x="144" fg:w="1"/><text x="46.2564%" y="47.50"></text></g><g><title>libsystem_malloc.dylib`nanov2_malloc (2 samples, 0.64%)</title><rect x="46.3259%" y="53" width="0.6390%" height="15" fill="rgb(221,5,38)" fg:x="145" fg:w="2"/><text x="46.5759%" y="63.50"></text></g><g><title>pbs_test`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (13 samples, 4.15%)</title><rect x="44.0895%" y="85" width="4.1534%" height="15" fill="rgb(247,18,42)" fg:x="138" fg:w="13"/><text x="44.3395%" y="95.50">pbs_t..</text></g><g><title>pbs_test`hashbrown::raw::RawTable&lt;T,A&gt;::reserve_rehash (8 samples, 2.56%)</title><rect x="45.6869%" y="69" width="2.5559%" height="15" fill="rgb(241,131,45)" fg:x="143" fg:w="8"/><text x="45.9369%" y="79.50">pb..</text></g><g><title>pbs_test`core::hash::BuildHasher::hash_one (4 samples, 1.28%)</title><rect x="46.9649%" y="53" width="1.2780%" height="15" fill="rgb(249,31,29)" fg:x="147" fg:w="4"/><text x="47.2149%" y="63.50"></text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (3 samples, 0.96%)</title><rect x="47.2843%" y="37" width="0.9585%" height="15" fill="rgb(225,111,53)" fg:x="148" fg:w="3"/><text x="47.5343%" y="47.50"></text></g><g><title>pbs_test`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (72 samples, 23.00%)</title><rect x="31.3099%" y="117" width="23.0032%" height="15" fill="rgb(238,160,17)" fg:x="98" fg:w="72"/><text x="31.5599%" y="127.50">pbs_test`&lt;alloc::vec::Vec&lt;T&gt; as allo..</text></g><g><title>pbs_test`proposer_builder_seperation::entities::builder::Builder::build_block (72 samples, 23.00%)</title><rect x="31.3099%" y="101" width="23.0032%" height="15" fill="rgb(214,148,48)" fg:x="98" fg:w="72"/><text x="31.5599%" y="111.50">pbs_test`proposer_builder_seperation..</text></g><g><title>pbs_test`proposer_builder_seperation::entities::builder::Builder::calculate_bid (19 samples, 6.07%)</title><rect x="48.2428%" y="85" width="6.0703%" height="15" fill="rgb(232,36,49)" fg:x="151" fg:w="19"/><text x="48.4928%" y="95.50">pbs_test..</text></g><g><title>pbs_test`&lt;rand_core::os::OsRng as rand_core::RngCore&gt;::try_fill_bytes (1 samples, 0.32%)</title><rect x="54.3131%" y="117" width="0.3195%" height="15" fill="rgb(209,103,24)" fg:x="170" fg:w="1"/><text x="54.5631%" y="127.50"></text></g><g><title>pbs_test`core::hash::BuildHasher::hash_one (2 samples, 0.64%)</title><rect x="54.6326%" y="117" width="0.6390%" height="15" fill="rgb(229,88,8)" fg:x="171" fg:w="2"/><text x="54.8826%" y="127.50"></text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (1 samples, 0.32%)</title><rect x="59.7444%" y="101" width="0.3195%" height="15" fill="rgb(213,181,19)" fg:x="187" fg:w="1"/><text x="59.9944%" y="111.50"></text></g><g><title>pbs_test`core::hash::BuildHasher::hash_one (16 samples, 5.11%)</title><rect x="60.0639%" y="101" width="5.1118%" height="15" fill="rgb(254,191,54)" fg:x="188" fg:w="16"/><text x="60.3139%" y="111.50">pbs_te..</text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (7 samples, 2.24%)</title><rect x="62.9393%" y="85" width="2.2364%" height="15" fill="rgb(241,83,37)" fg:x="197" fg:w="7"/><text x="63.1893%" y="95.50">p..</text></g><g><title>pbs_test`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (32 samples, 10.22%)</title><rect x="55.2716%" y="117" width="10.2236%" height="15" fill="rgb(233,36,39)" fg:x="173" fg:w="32"/><text x="55.5216%" y="127.50">pbs_test`hashbr..</text></g><g><title>pbs_test`hashbrown::raw::RawTable&lt;T,A&gt;::reserve_rehash (1 samples, 0.32%)</title><rect x="65.1757%" y="101" width="0.3195%" height="15" fill="rgb(226,3,54)" fg:x="204" fg:w="1"/><text x="65.4257%" y="111.50"></text></g><g><title>libsystem_platform.dylib`_platform_bzero$VARIANT$Haswell (1 samples, 0.32%)</title><rect x="65.1757%" y="85" width="0.3195%" height="15" fill="rgb(245,192,40)" fg:x="204" fg:w="1"/><text x="65.4257%" y="95.50"></text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (3 samples, 0.96%)</title><rect x="71.8850%" y="101" width="0.9585%" height="15" fill="rgb(238,167,29)" fg:x="225" fg:w="3"/><text x="72.1350%" y="111.50"></text></g><g><title>pbs_test`hashbrown::map::HashMap&lt;K,V,S,A&gt;::retain (50 samples, 15.97%)</title><rect x="65.4952%" y="117" width="15.9744%" height="15" fill="rgb(232,182,51)" fg:x="205" fg:w="50"/><text x="65.7452%" y="127.50">pbs_test`hashbrown::map::..</text></g><g><title>pbs_test`core::hash::BuildHasher::hash_one (27 samples, 8.63%)</title><rect x="72.8435%" y="101" width="8.6262%" height="15" fill="rgb(231,60,39)" fg:x="228" fg:w="27"/><text x="73.0935%" y="111.50">pbs_test`cor..</text></g><g><title>pbs_test`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (14 samples, 4.47%)</title><rect x="76.9968%" y="85" width="4.4728%" height="15" fill="rgb(208,69,12)" fg:x="241" fg:w="14"/><text x="77.2468%" y="95.50">pbs_t..</text></g><g><title>pbs_test`proposer_builder_seperation::setup::maintain::refill_transactions_default (1 samples, 0.32%)</title><rect x="81.4696%" y="117" width="0.3195%" height="15" fill="rgb(235,93,37)" fg:x="255" fg:w="1"/><text x="81.7196%" y="127.50"></text></g><g><title>pbs_test`rand_chacha::guts::refill_wide::impl_avx2 (3 samples, 0.96%)</title><rect x="81.7891%" y="117" width="0.9585%" height="15" fill="rgb(213,116,39)" fg:x="256" fg:w="3"/><text x="82.0391%" y="127.50"></text></g><g><title>pbs_test`proposer_builder_seperation::simulation::execute_simulation (177 samples, 56.55%)</title><rect x="26.8371%" y="133" width="56.5495%" height="15" fill="rgb(222,207,29)" fg:x="84" fg:w="177"/><text x="27.0871%" y="143.50">pbs_test`proposer_builder_seperation::simulation::execute_simulation</text></g><g><title>pbs_test`std::io::stdio::_print (2 samples, 0.64%)</title><rect x="82.7476%" y="117" width="0.6390%" height="15" fill="rgb(206,96,30)" fg:x="259" fg:w="2"/><text x="82.9976%" y="127.50"></text></g><g><title>pbs_test`&lt;&amp;std::io::stdio::Stdout as std::io::Write&gt;::write_fmt (2 samples, 0.64%)</title><rect x="82.7476%" y="101" width="0.6390%" height="15" fill="rgb(218,138,4)" fg:x="259" fg:w="2"/><text x="82.9976%" y="111.50"></text></g><g><title>pbs_test`core::fmt::write (2 samples, 0.64%)</title><rect x="82.7476%" y="85" width="0.6390%" height="15" fill="rgb(250,191,14)" fg:x="259" fg:w="2"/><text x="82.9976%" y="95.50"></text></g><g><title>pbs_test`&lt;std::io::Write::write_fmt::Adapter&lt;T&gt; as core::fmt::Write&gt;::write_str (2 samples, 0.64%)</title><rect x="82.7476%" y="69" width="0.6390%" height="15" fill="rgb(239,60,40)" fg:x="259" fg:w="2"/><text x="82.9976%" y="79.50"></text></g><g><title>pbs_test`&lt;std::io::stdio::StdoutLock as std::io::Write&gt;::write_all (2 samples, 0.64%)</title><rect x="82.7476%" y="53" width="0.6390%" height="15" fill="rgb(206,27,48)" fg:x="259" fg:w="2"/><text x="82.9976%" y="63.50"></text></g><g><title>libsystem_kernel.dylib`write (2 samples, 0.64%)</title><rect x="82.7476%" y="37" width="0.6390%" height="15" fill="rgb(225,35,8)" fg:x="259" fg:w="2"/><text x="82.9976%" y="47.50"></text></g><g><title>pbs_test`rand::rngs::adapter::reseeding::ReseedingCore&lt;R,Rsdr&gt;::reseed_and_generate (7 samples, 2.24%)</title><rect x="83.3866%" y="133" width="2.2364%" height="15" fill="rgb(250,213,24)" fg:x="261" fg:w="7"/><text x="83.6366%" y="143.50">p..</text></g><g><title>pbs_test`&lt;rand_core::os::OsRng as rand_core::RngCore&gt;::try_fill_bytes (7 samples, 2.24%)</title><rect x="83.3866%" y="117" width="2.2364%" height="15" fill="rgb(247,123,22)" fg:x="261" fg:w="7"/><text x="83.6366%" y="127.50">p..</text></g><g><title>libsystem_kernel.dylib`getentropy (6 samples, 1.92%)</title><rect x="83.7061%" y="101" width="1.9169%" height="15" fill="rgb(231,138,38)" fg:x="262" fg:w="6"/><text x="83.9561%" y="111.50">l..</text></g><g><title>pbs_test`rand_chacha::guts::refill_wide (2 samples, 0.64%)</title><rect x="85.6230%" y="133" width="0.6390%" height="15" fill="rgb(231,145,46)" fg:x="268" fg:w="2"/><text x="85.8730%" y="143.50"></text></g><g><title>pbs_test`pbs_test::main (302 samples, 96.49%)</title><rect x="1.2780%" y="149" width="96.4856%" height="15" fill="rgb(251,118,11)" fg:x="4" fg:w="302"/><text x="1.5280%" y="159.50">pbs_test`pbs_test::main</text></g><g><title>pbs_test`rand_chacha::guts::refill_wide::impl_avx2 (36 samples, 11.50%)</title><rect x="86.2620%" y="133" width="11.5016%" height="15" fill="rgb(217,147,25)" fg:x="270" fg:w="36"/><text x="86.5120%" y="143.50">pbs_test`rand_cha..</text></g><g><title>dyld`start (307 samples, 98.08%)</title><rect x="0.0000%" y="229" width="98.0831%" height="15" fill="rgb(247,81,37)" fg:x="0" fg:w="307"/><text x="0.2500%" y="239.50">dyld`start</text></g><g><title>pbs_test`main (303 samples, 96.81%)</title><rect x="1.2780%" y="213" width="96.8051%" height="15" fill="rgb(209,12,38)" fg:x="4" fg:w="303"/><text x="1.5280%" y="223.50">pbs_test`main</text></g><g><title>pbs_test`std::rt::lang_start_internal (303 samples, 96.81%)</title><rect x="1.2780%" y="197" width="96.8051%" height="15" fill="rgb(227,1,9)" fg:x="4" fg:w="303"/><text x="1.5280%" y="207.50">pbs_test`std::rt::lang_start_internal</text></g><g><title>pbs_test`std::rt::lang_start::_{{closure}} (303 samples, 96.81%)</title><rect x="1.2780%" y="181" width="96.8051%" height="15" fill="rgb(248,47,43)" fg:x="4" fg:w="303"/><text x="1.5280%" y="191.50">pbs_test`std::rt::lang_start::_{{closure}}</text></g><g><title>pbs_test`std::sys_common::backtrace::__rust_begin_short_backtrace (303 samples, 96.81%)</title><rect x="1.2780%" y="165" width="96.8051%" height="15" fill="rgb(221,10,30)" fg:x="4" fg:w="303"/><text x="1.5280%" y="175.50">pbs_test`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>pbs_test`rand::rngs::adapter::reseeding::fork::get_fork_counter (1 samples, 0.32%)</title><rect x="97.7636%" y="149" width="0.3195%" height="15" fill="rgb(210,229,1)" fg:x="306" fg:w="1"/><text x="98.0136%" y="159.50"></text></g><g><title>all (313 samples, 100%)</title><rect x="0.0000%" y="245" width="100.0000%" height="15" fill="rgb(222,148,37)" fg:x="0" fg:w="313"/><text x="0.2500%" y="255.50"></text></g><g><title>libsystem_kernel.dylib`__exit (6 samples, 1.92%)</title><rect x="98.0831%" y="229" width="1.9169%" height="15" fill="rgb(234,67,33)" fg:x="307" fg:w="6"/><text x="98.3331%" y="239.50">l..</text></g></svg></svg>