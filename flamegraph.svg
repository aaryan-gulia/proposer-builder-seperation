<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="310" onload="init(evt)" viewBox="0 0 1200 310" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="310" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="293.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="293.00"> </text><svg id="frames" x="10" width="1180" total_samples="44369"><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (9 samples, 0.02%)</title><rect x="0.2975%" y="117" width="0.0203%" height="15" fill="rgb(227,0,7)" fg:x="132" fg:w="9"/><text x="0.5475%" y="127.50"></text></g><g><title>proposer-builder-seperation`core::hash::BuildHasher::hash_one (79 samples, 0.18%)</title><rect x="0.3178%" y="117" width="0.1781%" height="15" fill="rgb(217,0,24)" fg:x="141" fg:w="79"/><text x="0.5678%" y="127.50"></text></g><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (33 samples, 0.07%)</title><rect x="0.4215%" y="101" width="0.0744%" height="15" fill="rgb(221,193,54)" fg:x="187" fg:w="33"/><text x="0.6715%" y="111.50"></text></g><g><title>proposer-builder-seperation`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (171 samples, 0.39%)</title><rect x="0.1149%" y="133" width="0.3854%" height="15" fill="rgb(248,212,6)" fg:x="51" fg:w="171"/><text x="0.3649%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`free_tiny (7 samples, 0.02%)</title><rect x="0.5049%" y="117" width="0.0158%" height="15" fill="rgb(208,68,35)" fg:x="224" fg:w="7"/><text x="0.7549%" y="127.50"></text></g><g><title>libsystem_malloc.dylib`free_small (5 samples, 0.01%)</title><rect x="0.6761%" y="85" width="0.0113%" height="15" fill="rgb(232,128,0)" fg:x="300" fg:w="5"/><text x="0.9261%" y="95.50"></text></g><g><title>proposer-builder-seperation`alloc::raw_vec::RawVec&lt;T,A&gt;::reserve::do_reserve_and_handle (7 samples, 0.02%)</title><rect x="0.6874%" y="85" width="0.0158%" height="15" fill="rgb(207,160,47)" fg:x="305" fg:w="7"/><text x="0.9374%" y="95.50"></text></g><g><title>proposer-builder-seperation`alloc::raw_vec::finish_grow (5 samples, 0.01%)</title><rect x="0.6919%" y="69" width="0.0113%" height="15" fill="rgb(228,23,34)" fg:x="307" fg:w="5"/><text x="0.9419%" y="79.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::insertion_sort_shift_left (8 samples, 0.02%)</title><rect x="0.7933%" y="69" width="0.0180%" height="15" fill="rgb(218,30,26)" fg:x="352" fg:w="8"/><text x="1.0433%" y="79.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::insertion_sort_shift_left (9 samples, 0.02%)</title><rect x="0.8497%" y="53" width="0.0203%" height="15" fill="rgb(220,122,19)" fg:x="377" fg:w="9"/><text x="1.0997%" y="63.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::recurse (82 samples, 0.18%)</title><rect x="0.7054%" y="85" width="0.1848%" height="15" fill="rgb(250,228,42)" fg:x="313" fg:w="82"/><text x="0.9554%" y="95.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::recurse (34 samples, 0.08%)</title><rect x="0.8136%" y="69" width="0.0766%" height="15" fill="rgb(240,193,28)" fg:x="361" fg:w="34"/><text x="1.0636%" y="79.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::recurse (6 samples, 0.01%)</title><rect x="0.8767%" y="53" width="0.0135%" height="15" fill="rgb(216,20,37)" fg:x="389" fg:w="6"/><text x="1.1267%" y="63.50"></text></g><g><title>proposer-builder-seperation`core::slice::sort::insertion_sort_shift_left (6 samples, 0.01%)</title><rect x="0.8767%" y="37" width="0.0135%" height="15" fill="rgb(206,188,39)" fg:x="389" fg:w="6"/><text x="1.1267%" y="47.50"></text></g><g><title>proposer-builder-seperation`core::hash::BuildHasher::hash_one (20 samples, 0.05%)</title><rect x="0.9128%" y="69" width="0.0451%" height="15" fill="rgb(217,207,13)" fg:x="405" fg:w="20"/><text x="1.1628%" y="79.50"></text></g><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (9 samples, 0.02%)</title><rect x="0.9376%" y="53" width="0.0203%" height="15" fill="rgb(231,73,38)" fg:x="416" fg:w="9"/><text x="1.1876%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`nanov2_malloc (10 samples, 0.02%)</title><rect x="0.9984%" y="53" width="0.0225%" height="15" fill="rgb(225,20,46)" fg:x="443" fg:w="10"/><text x="1.2484%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (5 samples, 0.01%)</title><rect x="1.0210%" y="53" width="0.0113%" height="15" fill="rgb(210,31,41)" fg:x="453" fg:w="5"/><text x="1.2710%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`tiny_malloc_should_clear (5 samples, 0.01%)</title><rect x="1.0210%" y="37" width="0.0113%" height="15" fill="rgb(221,200,47)" fg:x="453" fg:w="5"/><text x="1.2710%" y="47.50"></text></g><g><title>proposer-builder-seperation`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (82 samples, 0.18%)</title><rect x="0.8903%" y="85" width="0.1848%" height="15" fill="rgb(226,26,5)" fg:x="395" fg:w="82"/><text x="1.1403%" y="95.50"></text></g><g><title>proposer-builder-seperation`hashbrown::raw::RawTable&lt;T,A&gt;::reserve_rehash (52 samples, 0.12%)</title><rect x="0.9579%" y="69" width="0.1172%" height="15" fill="rgb(249,33,26)" fg:x="425" fg:w="52"/><text x="1.2079%" y="79.50"></text></g><g><title>proposer-builder-seperation`core::hash::BuildHasher::hash_one (15 samples, 0.03%)</title><rect x="1.0413%" y="53" width="0.0338%" height="15" fill="rgb(235,183,28)" fg:x="462" fg:w="15"/><text x="1.2913%" y="63.50"></text></g><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (9 samples, 0.02%)</title><rect x="1.0548%" y="37" width="0.0203%" height="15" fill="rgb(221,5,38)" fg:x="468" fg:w="9"/><text x="1.3048%" y="47.50"></text></g><g><title>libsystem_kernel.dylib`getentropy (592 samples, 1.33%)</title><rect x="76.1162%" y="37" width="1.3343%" height="15" fill="rgb(247,18,42)" fg:x="33772" fg:w="592"/><text x="76.3662%" y="47.50"></text></g><g><title>proposer-builder-seperation`&lt;rand_core::os::OsRng as rand_core::RngCore&gt;::try_fill_bytes (608 samples, 1.37%)</title><rect x="76.1072%" y="53" width="1.3703%" height="15" fill="rgb(241,131,45)" fg:x="33768" fg:w="608"/><text x="76.3572%" y="63.50"></text></g><g><title>proposer-builder-seperation`getrandom::imp::getrandom_inner (10 samples, 0.02%)</title><rect x="77.4550%" y="37" width="0.0225%" height="15" fill="rgb(249,31,29)" fg:x="34366" fg:w="10"/><text x="77.7050%" y="47.50"></text></g><g><title>proposer-builder-seperation`rand::rngs::adapter::reseeding::ReseedingCore&lt;R,Rsdr&gt;::reseed_and_generate (624 samples, 1.41%)</title><rect x="76.0824%" y="69" width="1.4064%" height="15" fill="rgb(225,111,53)" fg:x="33757" fg:w="624"/><text x="76.3324%" y="79.50"></text></g><g><title>proposer-builder-seperation`rand_chacha::guts::init_chacha (5 samples, 0.01%)</title><rect x="77.4775%" y="53" width="0.0113%" height="15" fill="rgb(238,160,17)" fg:x="34376" fg:w="5"/><text x="77.7275%" y="63.50"></text></g><g><title>proposer-builder-seperation`rand::rngs::adapter::reseeding::fork::get_fork_counter (21 samples, 0.05%)</title><rect x="77.4888%" y="69" width="0.0473%" height="15" fill="rgb(214,148,48)" fg:x="34381" fg:w="21"/><text x="77.7388%" y="79.50"></text></g><g><title>proposer-builder-seperation`rand_chacha::guts::refill_wide (136 samples, 0.31%)</title><rect x="77.5384%" y="69" width="0.3065%" height="15" fill="rgb(232,36,49)" fg:x="34403" fg:w="136"/><text x="77.7884%" y="79.50"></text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::entities::builder::Builder::calculate_bid (43,113 samples, 97.17%)</title><rect x="1.0751%" y="85" width="97.1692%" height="15" fill="rgb(209,103,24)" fg:x="477" fg:w="43113"/><text x="1.3251%" y="95.50">proposer-builder-seperation`proposer_builder_seperation::entities::builder::Builder::calculate_bid</text></g><g><title>proposer-builder-seperation`rand_chacha::guts::refill_wide::impl_avx2 (9,051 samples, 20.40%)</title><rect x="77.8449%" y="69" width="20.3994%" height="15" fill="rgb(229,88,8)" fg:x="34539" fg:w="9051"/><text x="78.0949%" y="79.50">proposer-builder-seperation`rand..</text></g><g><title>proposer-builder-seperation`rand::rngs::adapter::reseeding::fork::get_fork_counter (428 samples, 0.96%)</title><rect x="98.2465%" y="85" width="0.9646%" height="15" fill="rgb(213,181,19)" fg:x="43591" fg:w="428"/><text x="98.4965%" y="95.50"></text></g><g><title>proposer-builder-seperation`rand_chacha::guts::refill_wide (52 samples, 0.12%)</title><rect x="99.2112%" y="85" width="0.1172%" height="15" fill="rgb(254,191,54)" fg:x="44019" fg:w="52"/><text x="99.4612%" y="95.50"></text></g><g><title>proposer-builder-seperation`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (43,872 samples, 98.88%)</title><rect x="0.5206%" y="117" width="98.8798%" height="15" fill="rgb(241,83,37)" fg:x="231" fg:w="43872"/><text x="0.7706%" y="127.50">proposer-builder-seperation`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter</text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::entities::builder::Builder::build_block (43,860 samples, 98.85%)</title><rect x="0.5477%" y="101" width="98.8528%" height="15" fill="rgb(233,36,39)" fg:x="243" fg:w="43860"/><text x="0.7977%" y="111.50">proposer-builder-seperation`proposer_builder_seperation::entities::builder::Builder::build_block</text></g><g><title>proposer-builder-seperation`rand_chacha::guts::refill_wide::impl_avx2 (32 samples, 0.07%)</title><rect x="99.3284%" y="85" width="0.0721%" height="15" fill="rgb(226,3,54)" fg:x="44071" fg:w="32"/><text x="99.5784%" y="95.50"></text></g><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (10 samples, 0.02%)</title><rect x="99.5560%" y="101" width="0.0225%" height="15" fill="rgb(245,192,40)" fg:x="44172" fg:w="10"/><text x="99.8060%" y="111.50"></text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::entities::traits::Proposer::run_auction (44,112 samples, 99.42%)</title><rect x="0.5049%" y="133" width="99.4208%" height="15" fill="rgb(238,167,29)" fg:x="224" fg:w="44112"/><text x="0.7549%" y="143.50">proposer-builder-seperation`proposer_builder_seperation::entities::traits::Proposer::run_auction</text></g><g><title>proposer-builder-seperation`hashbrown::map::HashMap&lt;K,V,S,A&gt;::retain (233 samples, 0.53%)</title><rect x="99.4005%" y="117" width="0.5251%" height="15" fill="rgb(232,182,51)" fg:x="44103" fg:w="233"/><text x="99.6505%" y="127.50"></text></g><g><title>proposer-builder-seperation`core::hash::BuildHasher::hash_one (154 samples, 0.35%)</title><rect x="99.5785%" y="101" width="0.3471%" height="15" fill="rgb(231,60,39)" fg:x="44182" fg:w="154"/><text x="99.8285%" y="111.50"></text></g><g><title>proposer-builder-seperation`&lt;std::hash::random::DefaultHasher as core::hash::Hasher&gt;::write (55 samples, 0.12%)</title><rect x="99.8017%" y="85" width="0.1240%" height="15" fill="rgb(208,69,12)" fg:x="44281" fg:w="55"/><text x="100.0517%" y="95.50"></text></g><g><title>proposer-builder-seperation`&lt;hashbrown::map::HashMap&lt;K,V,S,A&gt; as core::iter::traits::collect::Extend&lt;(K,V)&gt;&gt;::extend (5 samples, 0.01%)</title><rect x="99.9279%" y="117" width="0.0113%" height="15" fill="rgb(235,93,37)" fg:x="44337" fg:w="5"/><text x="100.1779%" y="127.50"></text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::setup::maintain::refill_transactions_default (9 samples, 0.02%)</title><rect x="99.9256%" y="133" width="0.0203%" height="15" fill="rgb(213,116,39)" fg:x="44336" fg:w="9"/><text x="100.1756%" y="143.50"></text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::simulation::execute_simulation (44,365 samples, 99.99%)</title><rect x="0.0023%" y="149" width="99.9910%" height="15" fill="rgb(222,207,29)" fg:x="1" fg:w="44365"/><text x="0.2523%" y="159.50">proposer-builder-seperation`proposer_builder_seperation::simulation::execute_simulation</text></g><g><title>proposer-builder-seperation`rand_chacha::guts::refill_wide::impl_avx2 (17 samples, 0.04%)</title><rect x="99.9549%" y="133" width="0.0383%" height="15" fill="rgb(206,96,30)" fg:x="44349" fg:w="17"/><text x="100.2049%" y="143.50"></text></g><g><title>all (44,369 samples, 100%)</title><rect x="0.0000%" y="261" width="100.0000%" height="15" fill="rgb(218,138,4)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="271.50"></text></g><g><title>dyld`start (44,369 samples, 100.00%)</title><rect x="0.0000%" y="245" width="100.0000%" height="15" fill="rgb(250,191,14)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="255.50">dyld`start</text></g><g><title>proposer-builder-seperation`main (44,369 samples, 100.00%)</title><rect x="0.0000%" y="229" width="100.0000%" height="15" fill="rgb(239,60,40)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="239.50">proposer-builder-seperation`main</text></g><g><title>proposer-builder-seperation`std::rt::lang_start_internal (44,369 samples, 100.00%)</title><rect x="0.0000%" y="213" width="100.0000%" height="15" fill="rgb(206,27,48)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="223.50">proposer-builder-seperation`std::rt::lang_start_internal</text></g><g><title>proposer-builder-seperation`std::rt::lang_start::_{{closure}} (44,369 samples, 100.00%)</title><rect x="0.0000%" y="197" width="100.0000%" height="15" fill="rgb(225,35,8)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="207.50">proposer-builder-seperation`std::rt::lang_start::_{{closure}}</text></g><g><title>proposer-builder-seperation`std::sys_common::backtrace::__rust_begin_short_backtrace (44,369 samples, 100.00%)</title><rect x="0.0000%" y="181" width="100.0000%" height="15" fill="rgb(250,213,24)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="191.50">proposer-builder-seperation`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>proposer-builder-seperation`proposer_builder_seperation::main (44,369 samples, 100.00%)</title><rect x="0.0000%" y="165" width="100.0000%" height="15" fill="rgb(247,123,22)" fg:x="0" fg:w="44369"/><text x="0.2500%" y="175.50">proposer-builder-seperation`proposer_builder_seperation::main</text></g></svg></svg>